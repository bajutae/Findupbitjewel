# 🔍💎 업비트 숨은 보석 찾기 - 최소 의존성
# 업비트 API 호출
requests>=2.31.0

# 데이터 처리
pandas>=2.1.0
numpy>=1.24.0

# 기술적 분석
ta>=0.10.0

# 환경 변수 로드 (선택사항)
python-dotenv>=1.0.0

# 웹 인터페이스 (선택사항)
flask>=2.3.0

# 업비트 API 직접 호출 (ccxt 대신)
# ccxt는 무거우므로 requests로 직접 API 호출 